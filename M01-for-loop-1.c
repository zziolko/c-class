#include <stdio.h>

int main(){

  int iter;
  int max_iter = 10;

  /* definition of the loop */
  for(iter = 0; iter < max_iter; iter++)
  {
    /* start of the body */
  
    printf("Value of iter is %d \n", iter);

    /* end of the body */
  }

  return 0;
}
